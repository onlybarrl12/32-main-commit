# Local PostgreSQL data directory (machine-specific binary data, see CLAUDE.md §8)
.pgdata/

# Node / npm
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm/

# Next.js
webapp/.next/
webapp/out/
webapp/build/
.vercel/

# TypeScript
*.tsbuildinfo
next-env.d.ts.bak

# Env / secrets
.env
.env.local
.env.*.local
webapp/.env
webapp/.env.local
webapp/.env.*.local

# Local test/dev credentials (plaintext passwords — see TEST_CREDENTIALS.local.json)
*.local.json

# Prisma
webapp/prisma/*.db
webapp/prisma/migrations/dev/

# Uploaded budget attachments (runtime data, not source)
webapp/uploads/
uploads/

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs
*.log

# Watchdog runtime lock file (see RESTART_RESILIENCE.md) — machine state, not source
scripts/*.lock
